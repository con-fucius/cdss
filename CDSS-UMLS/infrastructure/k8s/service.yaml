apiVersion: v1
kind: Service
metadata:
  name: umls-cdss-api
  labels:
    app: umls-cdss-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: umls-cdss-api

